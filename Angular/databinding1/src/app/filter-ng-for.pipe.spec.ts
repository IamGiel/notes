import { FilterNgForPipe } from './filter-ng-for.pipe';

describe('FilterNgForPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterNgForPipe();
    expect(pipe).toBeTruthy();
  });
});
